import { SearchOptimizationPipe } from './search-optimization.pipe';

describe('SearchOptimizationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOptimizationPipe();
    expect(pipe).toBeTruthy();
  });
});
